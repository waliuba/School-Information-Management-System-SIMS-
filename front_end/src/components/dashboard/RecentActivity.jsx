export default function RecentActivity() {
  return (
    <section className="activity-panel">
      <h2>Recent backend activity</h2>
      <p>
        This panel is reserved for backend-provided audit or activity data in a later phase.
        React will display records returned by an API endpoint rather than generating activity locally.
      </p>
    </section>
  );
}
